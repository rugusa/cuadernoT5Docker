services:
  db:
    image: rugusa/calculadoraD
    container_name: contenedorCalculadoraD
    ports:
      - "3306:3306"

  php:
    image: rugusa/calculadoraD
    container_name: contenedorcalculadoraD
    ports:
      - "8080:80"
    depends_on:
      - db

  phpmyadmin:
     image: phpmyadmin/phpmyadmin:latest
     container_name: contenedorPhpMyAdmin
     environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: contrasenyaRoot
     ports:
      - "8081:80"
     depends_on:
       - db
